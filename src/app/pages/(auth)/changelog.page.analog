<script lang="ts">
	import { navigationEntries } from '../../utils/navigation-entry';
	import { routeTitle } from '../../utils/route-title';

	export const routeMeta = {
		title: routeTitle(navigationEntries.changelog.title),
		data: navigationEntries.changelog,
	};
</script>

<template>Hi changelog</template>
