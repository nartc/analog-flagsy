<script lang="ts">
	import { navigationEntries } from '../../utils/navigation-entry';
	import { routeTitle } from '../../utils/route-title';

	export const routeMeta = {
		title: routeTitle(navigationEntries.feature.title),
		data: navigationEntries.feature,
	};
</script>

<template>Hi features</template>
