<script lang="ts">
	import { navigationEntries } from '../../utils/navigation-entry';
	import { routeTitle } from '../../utils/route-title';

	export const routeMeta = {
		title: routeTitle(navigationEntries.environment.title),
		data: navigationEntries.environment,
	};
</script>

<template>Hi environments</template>
